import styled from 'styled-components';
import {View} from 'react-native';
import {Text} from '@rneui/base';

export const ErrorComponentContainer = styled(View)`
  flex: 1;
`;
export const ErrorText = styled(Text)`
  flex: 1;
  align-self: center;
  justify-content: center;
`;
